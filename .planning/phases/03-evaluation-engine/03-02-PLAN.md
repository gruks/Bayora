---
phase: 03-evaluation-engine
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - services/blue-agent/src/blue_agent/classifiers/toxigen_classifier.py
  - services/blue-agent/src/blue_agent/classifiers/ensemble.py
  - services/blue-agent/scripts/train_toxigen.py
  - services/blue-agent/tests/test_classifiers.py
  - services/blue-agent/pyproject.toml
autonomous: true

must_haves:
  truths:
    - "User can classify model outputs using a ToxiGen-trained classifier for implicit toxicity detection"
    - "User can get combined classification from both HH-RLHF and ToxiGen classifiers via ensemble"
    - "Ensemble classifier reports agreement/disagreement between source classifiers"
  artifacts:
    - path: "services/blue-agent/src/blue_agent/classifiers/toxigen_classifier.py"
      provides: "ToxiGen trained DistilBERT classifier implementation"
      contains: "class ToxiGenClassifier"
    - path: "services/blue-agent/src/blue_agent/classifiers/ensemble.py"
      provides: "Combined classifier that aggregates HH-RLHF and ToxiGen outputs"
      contains: "class EnsembleClassifier"
    - path: "services/blue-agent/scripts/train_toxigen.py"
      provides: "Training script for ToxiGen dataset"
      contains: "def main"
  key_links:
    - from: "services/blue-agent/src/blue_agent/classifiers/ensemble.py"
      to: "services/blue-agent/src/blue_agent/classifiers/hh_rlhf_classifier.py"
      via: "composes HHRLHFClassifier and ToxiGenClassifier"
      pattern: "HHRLHFClassifier|ToxiGenClassifier"
    - from: "services/blue-agent/src/blue_agent/classifiers/ensemble.py"
      to: "packages/centinela-core/src/centinela/models/evaluation.py"
      via: "returns ClassificationResult with model_source='ensemble'"
      pattern: "ClassificationResult\\("
---

<objective>
Implement the ToxiGen classifier and an ensemble classifier that combines HH-RLHF and ToxiGen outputs. The ensemble addresses over-refusal (false positives) by requiring agreement between both classifiers for high-confidence harm classifications.

Purpose: BLUE-03 requires ToxiGen training. The ensemble provides robust classification by combining general harmlessness (HH-RLHF) with implicit toxicity detection (ToxiGen), mitigating the over-refusal pitfall identified in research.
Output: ToxiGenClassifier, EnsembleClassifier, and training script for ToxiGen dataset.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-evaluation-engine/03-RESEARCH.md
@.planning/phases/03-evaluation-engine/03-01-PLAN.md
@services/blue-agent/src/blue_agent/classifiers/base.py
@services/blue-agent/src/blue_agent/classifiers/hh_rlhf_classifier.py
@packages/centinela-core/src/centinela/models/evaluation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ToxiGen classifier with training script</name>
  <files>
    services/blue-agent/src/blue_agent/classifiers/toxigen_classifier.py
    services/blue-agent/scripts/train_toxigen.py
    services/blue-agent/pyproject.toml
  </files>
  <action>
    1. Implement ToxiGenClassifier:
       - Create `services/blue-agent/src/blue_agent/classifiers/toxigen_classifier.py`
       - Class `ToxiGenClassifier(SafetyClassifier)` that loads a DistilBERT sequence classification model fine-tuned on ToxiGen
       - __init__ accepts `model_path: str` and optional `device: str = "cpu"`
       - Use `transformers.pipeline("text-classification", model=model_path, tokenizer=model_path, device=device)` for inference
       - `classify(self, response_text: str) -> ClassificationResult`:
         - Run pipeline on response_text
         - Map pipeline output to "harm"/"safe" labels
         - Return ClassificationResult with label, confidence, model_source="toxigen"
       - Handle model loading errors gracefully

    2. Create training script:
       - Create `services/blue-agent/scripts/train_toxigen.py`
       - Script uses `datasets.load_dataset("toxigen/toxigen-data")` for the labeled dataset
       - ToxiGen format: text + label (0=benign, 1=toxic) — no conversion needed unlike HH-RLHF
       - Tokenize with `distilbert/distilbert-base-uncased` tokenizer (truncation=True, max_length=512)
       - Fine-tune using `transformers.Trainer` with same hyperparameters as HH-RLHF training:
         - learning_rate=2e-5, per_device_train_batch_size=16, num_train_epochs=3, weight_decay=0.01
         - eval_strategy="epoch", save_strategy="epoch", load_best_model_at_end=True
       - Add CLI argument `--output-dir` (default: "./models/toxigen-classifier")
       - Add note: "Requires HuggingFace access approval for toxigen/toxigen-data. Request access at https://huggingface.co/datasets/toxigen/toxigen-data"
       - Add fallback: if dataset access denied, use a small mock dataset for testing the training pipeline
       - Add entry point in pyproject.toml: `train-toxigen = "scripts.train_toxigen:main"`

    3. Add `datasets>=3.0` to blue-agent/pyproject.toml dependencies (needed for training scripts)
  </action>
  <verify>
    - `uv run --package blue-agent python -c "from blue_agent.classifiers.toxigen_classifier import ToxiGenClassifier; print('Import OK')"` succeeds
    - `uv run --package blue-agent python services/blue-agent/scripts/train_toxigen.py --help` shows usage
    - `ruff check services/blue-agent/src/blue_agent/classifiers/toxigen_classifier.py services/blue-agent/scripts/train_toxigen.py` passes
    - `mypy services/blue-agent/src/blue_agent/classifiers/toxigen_classifier.py` passes
  </verify>
  <done>
    - ToxiGenClassifier implements SafetyClassifier interface
    - Training script exists with --output-dir CLI argument and HF access note
    - datasets dependency added to pyproject.toml
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement ensemble classifier with agreement scoring</name>
  <files>
    services/blue-agent/src/blue_agent/classifiers/ensemble.py
    services/blue-agent/tests/test_classifiers.py
  </files>
  <action>
    1. Implement EnsembleClassifier:
       - Create `services/blue-agent/src/blue_agent/classifiers/ensemble.py`
       - Class `EnsembleClassifier(SafetyClassifier)` that composes multiple SafetyClassifier instances
       - __init__ accepts `classifiers: list[SafetyClassifier]` and optional `agreement_threshold: float = 0.5`
       - `classify(self, response_text: str) -> ClassificationResult`:
         - Run classify() on each sub-classifier with the same response_text
         - Aggregate results:
           - If all classifiers agree on label -> return that label with mean confidence
           - If classifiers disagree -> return the more conservative label ("harm" if any says harm) with reduced confidence (min confidence * 0.8)
         - Return ClassificationResult with model_source="ensemble"
       - Add `get_sub_results(self, response_text: str) -> list[ClassificationResult]` method for debugging/transparency
       - This method returns the individual classification results from each sub-classifier

    2. Add tests to `services/blue-agent/tests/test_classifiers.py`:
       - Test ToxiGenClassifier with mock pipeline (same pattern as HHRLHFClassifier tests)
       - Test EnsembleClassifier with two mock classifiers:
         - Both agree "safe" -> result is "safe" with mean confidence
         - Both agree "harm" -> result is "harm" with mean confidence
         - Disagree (one safe, one harm) -> result is "harm" with reduced confidence
       - Test EnsembleClassifier.get_sub_results() returns individual results
       - Test that ensemble classify() accepts only response_text (no prompt)
  </action>
  <verify>
    - `uv run --package blue-agent pytest services/blue-agent/tests/test_classifiers.py -v` passes (all classifier tests including ensemble)
    - `uv run --package blue-agent python -c "from blue_agent.classifiers.ensemble import EnsembleClassifier; print('Import OK')"` succeeds
    - `ruff check services/blue-agent/src/blue_agent/classifiers/ensemble.py` passes
    - `mypy services/blue-agent/src/blue_agent/classifiers/ensemble.py` passes
  </verify>
  <done>
    - EnsembleClassifier composes multiple SafetyClassifier instances
    - Agreement logic: unanimous -> mean confidence, disagreement -> conservative label with reduced confidence
    - get_sub_results() provides transparency into individual classifier outputs
    - All tests pass, ruff and mypy clean
  </done>
</task>

</tasks>

<verification>
1. ToxiGenClassifier can classify responses (tested with mock pipeline)
2. EnsembleClassifier correctly aggregates multiple classifier outputs
3. Ensemble disagreement handling returns conservative (harm) label with reduced confidence
4. All classifiers accept only response_text, never prompt
5. Training scripts for both classifiers exist and show correct usage with --help
</verification>

<success_criteria>
- BLUE-03 satisfied: ToxiGenClassifier exists with training script for ToxiGen dataset
- Ensemble classifier combines HH-RLHF and ToxiGen outputs with agreement logic
- All classifiers implement the SafetyClassifier interface
- No classifier method accepts a prompt parameter
</success_criteria>

<output>
After completion, create `.planning/phases/03-evaluation-engine/03-02-SUMMARY.md`
</output>
