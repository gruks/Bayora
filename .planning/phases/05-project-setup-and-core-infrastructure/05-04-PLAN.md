---
phase: 05-project-setup-and-core-infrastructure
plan: 04
type: execute
wave: 3
depends_on:
  - 05-03
files_modified:
  - packages/centinela-core/src/centinela/audit/__init__.py
  - packages/centinela-core/src/centinela/audit/chain.py
  - packages/centinela-core/src/centinela/provenance/__init__.py
  - packages/centinela-core/src/centinela/provenance/tracker.py
autonomous: true

must_haves:
  truths:
    - "Audit entries hash-chain with SHA-256"
    - "Chain integrity verifiable in O(log n)"
    - "Provenance tracks parent-child lineage"
    - "Backward/forward tracing works"
  artifacts:
    - path: "packages/centinela-core/src/centinela/audit/chain.py"
      provides: "AuditChain with SHA-256 hash chaining, append, verify methods"
    - path: "packages/centinela-core/src/centinela/provenance/tracker.py"
      provides: "ProvenanceTracker with graph storage, lineage queries"
  key_links:
    - from: "audit/chain.py"
      to: "models/types.py"
      via: "AuditRecord import"
    - from: "provenance/tracker.py"
      to: "models/types.py"
      via: "Session import"
---

<objective>
Implement tamper-evident audit logging and data lineage tracking.

Purpose: Enable forensic traceability and cryptographic verification
Output: Audit and provenance modules under centinela-core
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-project-setup-and-core-infrastructure/05-RESEARCH.md

# Note: Phase 4 completed audit chain - this builds on that foundation with generic audit module
</context>

<tasks>

<task type="auto">
  <name>Create audit chain module</name>
  <files>packages/centinela-core/src/centinela/audit/chain.py</files>
  <action>
Create audit/chain.py with:
- AuditEntry class: entry_id (UUID), timestamp (datetime), event_type (EventType), actor (str), resource (str), payload (dict), prev_hash (str | None)
- AuditChain class: _entries (list), append(event_type, actor, resource, payload) -> AuditEntry, verify() -> bool, get_root_hash() -> str
- hash computation: SHA-256 of (entry_id + timestamp.isoformat() + event_type + actor + payload_str + prev_hash)

Implement verify() that traverses chain and validates each hash matches computed hash.
  </action>
  <verify>chain.append() creates linked entries, verify() returns True for valid chain</verify>
  <done>Audit entries hash-chain correctly</done>
</task>

<task type="auto">
  <name>Create audit module __init__</name>
  <files>packages/centinela-core/src/centinela/audit/__init__.py</files>
  <action>
Create audit/__init__.py with:
- from .chain import AuditChain, AuditEntry
- AuditClient interface for async append operations
- __all__ exports

Include basic async wrapper using asyncio.to_thread for sync operations.
  </action>
  <verify>from centinela.audit import AuditChain works</verify>
  <done>Audit module exports correctly</done>
</task>

<task type="auto">
  <name>Create provenance tracker</name>
  <files>packages/centinela-core/src/centinela/provenance/tracker.py</files>
  <action>
Create provenance/tracker.py with:
- ProvenanceNode: artifact_id, artifact_type, metadata (dict), parents (list[str]), created_at
- ProvenanceTracker class: _graph (dict[str, ProvenanceNode]), add_artifact(id, type, metadata, parents) -> ProvenanceNode
- trace_backward(artifact_id) -> list[ProvenanceNode]: follow parent links to origin
- trace_forward(artifact_id) -> list[ProvenanceNode]: find all artifacts derived from given artifact
- get_isolation_boundary_crossings(artifact_id, boundary) -> list[dict]: detect when artifact crosses tenant/namespace boundaries

Use dict-based adjacency list for storage (not NetworkX - overkill for simple cases).
  </action>
  <verify>trace_backward() returns correct parent chain</verify>
  <done>Provenance lineage tracking works</done>
</task>

<task type="auto">
  <name>Create provenance module __init__</name>
  <files>packages/centinela-core/src/centinela/provenance/__init__.py</files>
  <action>
Create provenance/__init__.py with:
- from .tracker import ProvenanceTracker, ProvenanceNode
- __all__ exports
  </action>
  <verify>from centinela.provenance import ProvenanceTracker works</verify>
  <done>Provenance module exports correctly</done>
</task>

</tasks>

<verification>
- Run `ruff check packages/centinela-core/src/centinela/audit/ packages/centinela-core/src/centinela/provenance/`
- Run `mypy packages/centinela-core/src/centinela/audit/ packages/centinela-core/src/centinela/provenance/`
- Test chain: create 3 entries, verify() should return True; tamper with entry, verify() should return False
</verification>

<success_criteria>
- Audit chain uses SHA-256 hash chaining
- Chain integrity verifiable (tampering detected)
- Provenance backward trace returns correct parent chain
- Provenance forward trace returns all derived artifacts
- Isolation boundary crossing detection works
</success_criteria>

<output>
After completion, create `.planning/phases/05-project-setup-and-core-infrastructure/05-04-SUMMARY.md`
</output>