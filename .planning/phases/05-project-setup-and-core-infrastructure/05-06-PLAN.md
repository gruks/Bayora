---
phase: 05-project-setup-and-core-infrastructure
plan: 06
type: execute
wave: 5
depends_on:
  - 05-05
files_modified:
  - packages/centinela-core/src/centinela/resources/__init__.py
  - packages/centinela-core/src/centinela/resources/governor.py
  - packages/centinela-core/src/centinela/orchestrator/__init__.py
  - packages/centinela-core/src/centinela/orchestrator/session.py
autonomous: true

must_haves:
  truths:
    - "Resource governor enforces cgroup v2 limits"
    - "Orchestrator manages session lifecycle"
    - "Prometheus metrics expose resource usage"
  artifacts:
    - path: "packages/centinela-core/src/centinela/resources/governor.py"
      provides: "ResourceGovernor with CPU, memory, I/O limits"
    - path: "packages/centinela-core/src/centinela/orchestrator/session.py"
      provides: "SessionManager with lifecycle (CREATED->RUNNING->COMPLETED/TERMINATED)"
  key_links:
    - from: "resources/governor.py"
      to: "models/types.py"
      via: "ResourceQuota import"
    - from: "orchestrator/session.py"
      to: "enums.py"
      via: "SessionState import"
---

<objective>
Implement cgroup v2 resource governance and session lifecycle management.

Purpose: Enable resource quota enforcement and orchestrator core
Output: Resources and orchestrator modules under centinela-core
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-project-setup-and-core-infrastructure/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create resource governor for cgroup v2</name>
  <files>packages/centinela-core/src/centinela/resources/governor.py</files>
  <action>
Create resources/governor.py with:
- ResourceQuotaSpec: cpu_quota, memory_high, memory_max, io_weight, pids_max
- ResourceGovernor class:
  - __init__(quota: ResourceQuotaSpec)
  - generate_cgroup_spec() -> dict: produce cgroup v2 settings dict
  - validate_quota() -> list[str]: return validation errors (cpu_quota must be positive, memory limits within bounds)
- Prometheus metrics: resource_limit, resource_usage, resource_violations

CPU uses cpu.max (period/quota format: "quota/period"), memory uses memory.high and memory.max.
  </action>
  <verify>generate_cgroup_spec() produces valid cgroup v2 settings</verify>
  <done>Resource governor produces cgroup specs</done>
</task>

<task type="auto">
  <name>Create resources module __init__</name>
  <files>packages/centinela-core/src/centinela/resources/__init__.py</files>
  <action>
Create resources/__init__.py with:
- from .governor import ResourceGovernor, ResourceQuotaSpec
- Metrics endpoint setup for prometheus_client
- __all__ exports
  </action>
  <verify>from centinela.resources import ResourceGovernor works</verify>
  <done>Resources module exports correctly</done>
</task>

<task type="auto">
  <name>Create orchestrator session manager</name>
  <files>packages/centinela-core/src/centinela/orchestrator/session.py</files>
  <action>
Create orchestrator/session.py with:
- SessionManager class:
  - create_session(config: PlatformConfig, tenant_id: str) -> Session: generates UUID v4, sets state=CREATED, creates SecretRef
  - start_session(session_id: UUID) -> Session: transitions CREATED->RUNNING, allocates resources
  - complete_session(session_id: UUID) -> Session: transitions RUNNING->COMPLETED
  - terminate_session(session_id: UUID, reason: str) -> Session: transitions to TERMINATED, triggers cleanup
  - timeout_session(session_id: UUID) -> Session: auto-timeout after 3600s max
- get_session(session_id: UUID) -> Session | None
- list_sessions(tenant_id: str, state: SessionState | None) -> list[Session]

Include timeout enforcement (3600s max from ROADMAP).
  </action>
  <verify>create_session creates session with UUID, start_session transitions to RUNNING</verify>
  <done>Session lifecycle management works</done>
</task>

<task type="auto">
  <name>Create orchestrator module __init__</name>
  <files>packages/centinela-core/src/centinela/orchestrator/__init__.py</files>
  <action>
Create orchestrator/__init__.py with:
- from .session import SessionManager, Session
- SessionLifecycle interface (ABC)
- Prometheus metrics: session_count, session_duration, session_termination_reason
- __all__ exports
  </action>
  <verify>from centinela.orchestrator import SessionManager works</verify>
  <done>Orchestrator module exports correctly</done>
</task>

</tasks>

<verification>
- Run `ruff check packages/centinela-core/src/centinela/resources/ packages/centinela-core/src/centinela/orchestrator/`
- Run `mypy packages/centinela-core/src/centinela/resources/ packages/centinela-core/src/centinela/orchestrator/`
- Test session lifecycle: create -> start -> complete -> get returns state=COMPLETED
</verification>

<success_criteria>
- ResourceGovernor generates valid cgroup v2 specs
- Session lifecycle: CREATED -> RUNNING -> COMPLETED/TERMINATED
- UUID v4 generation for session IDs
- 3600s max timeout enforcement
- Prometheus metrics export correctly
</success_criteria>

<output>
After completion, create `.planning/phases/05-project-setup-and-core-infrastructure/05-06-SUMMARY.md`
</output>