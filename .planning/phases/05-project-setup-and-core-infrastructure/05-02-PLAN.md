---
phase: 05-project-setup-and-core-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/centinela-core/src/centinela/__init__.py
  - packages/centinela-core/src/centinela/enums.py
  - packages/centinela-core/src/centinela/models/__init__.py
  - packages/centinela-core/src/centinela/models/types.py
autonomous: true

must_haves:
  truths:
    - "All enums are importable from centinela.enums"
    - "All type models are importable from centinela.models.types"
    - "Type annotations work with mypy"
  artifacts:
    - path: "packages/centinela-core/src/centinela/enums.py"
      provides: "SessionState, EventType, ResourceUnit, SecurityLevel enums"
    - path: "packages/centinela-core/src/centinela/models/types.py"
      provides: "Session, PodSpec, ContainerConfig, SecretRef type definitions"
  key_links:
    - from: "packages/centinela-core/src/centinela/__init__.py"
      to: "enums.py, models/types.py"
      via: "__all__ exports"
---

<objective>
Create core data models and enums shared across all 8 modules.

Purpose: Provide type safety and consistent enums for orchestrator, config, secrets, audit, provenance, anomaly, network, resources
Output: Enums and type definitions under centinela-core
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-project-setup-and-core-infrastructure/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create core enums for platform</name>
  <files>packages/centinela-core/src/centinela/enums.py</files>
  <action>
Create centinela-core/src/centinela/enums.py with:
- SessionState enum: CREATED, RUNNING, COMPLETED, TERMINATED, FAILED
- EventType enum: SESSION_START, SESSION_END, PROMPT_SUBMITTED, RESPONSE_RECEIVED, CONTAINER_START, CONTAINER_STOP, SECRET_ROTATE, CONFIG_CHANGE
- ResourceUnit enum: CPU_CORES, MEMORY_MB, DISK_MB, NETWORK_BW_MBPS
- SecurityLevel enum: STANDARD, ENHANCED, RESTRICTED
- NetworkPolicyType enum: ALLOW_ALL, DENY_ALL, WHITELIST
- AuditAction enum: CREATE, READ, UPDATE, DELETE, EXECUTE

Use Python's StrEnum (3.11+) or str-based enums for JSON serialization compatibility.
  </action>
  <verify>from centinela.enums import SessionState, EventType, SecurityLevel works</verify>
  <done>All enums importable and serialize to JSON correctly</done>
</task>

<task type="auto">
  <name>Create core type models</name>
  <files>packages/centinela-core/src/centinela/models/types.py</files>
  <action>
Create centinela-core/src/centinela/models/types.py with:
- Session: id (UUID), state (SessionState), created_at, updated_at, config (dict), metadata (dict)
- PodSpec: name, namespace, containers (list[ContainerConfig]), labels, annotations
- ContainerConfig: name, image, command, env_vars (dict[str,str]), resources (dict), security_context (dict)
- SecretRef: name, path (tmpfs mount point), encryption_key_id
- NetworkConfig: interface_name, allowed_peers (list[str]), endpoint, peer_public_key
- ResourceQuota: cpu_limit, memory_limit, disk_limit, network_bw_limit
- AuditRecord: entry_id, timestamp, event_type, actor, resource, payload, prev_hash

Use pydantic BaseModel with frozen=True for immutability, validate_defaults=True.
  </action>
  <verify>from centinela.models.types import Session, PodSpec, ContainerConfig works</verify>
  <done>All type models importable and validate correctly</done>
</task>

<task type="auto">
  <name>Update centinela-core exports</name>
  <files>packages/centinela-core/src/centinela/__init__.py</files>
  <action>
Update __init__.py to export:
- from .enums import (all enums)
- from .models.types import (all type models)

Update __all__ to list all exported names.
  </action>
  <verify>import centinela works, from centinela import SessionState works</verify>
  <done>All public API accessible from package root</done>
</task>

</tasks>

<verification>
- Run `ruff check packages/centinela-core/src/centinela/` - should pass
- Run `mypy packages/centinela-core/src/centinela/` - should pass with strict
- Run `python -c "from centinela import Session, SessionState; print('OK')"` - should print OK
</verification>

<success_criteria>
- All 8 enums (SessionState, EventType, ResourceUnit, SecurityLevel, NetworkPolicyType, AuditAction) defined
- All 7 type models (Session, PodSpec, ContainerConfig, SecretRef, NetworkConfig, ResourceQuota, AuditRecord) defined
- All imports work from package root
</success_criteria>

<output>
After completion, create `.planning/phases/05-project-setup-and-core-infrastructure/05-02-SUMMARY.md`
</output>