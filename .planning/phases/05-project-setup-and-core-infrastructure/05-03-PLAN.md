---
phase: 05-project-setup-and-core-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - 05-02
files_modified:
  - packages/centinela-core/src/centinela/config/__init__.py
  - packages/centinela-core/src/centinela/config/models.py
  - packages/centinela-core/src/centinela/secrets/__init__.py
  - packages/centinela-core/src/centinela/secrets/manager.py
autonomous: true

must_haves:
  truths:
    - "Config loads from YAML/JSON with env var substitution"
    - "Secrets encrypt/decrypt with AES-256-GCM"
    - "Resource limits validate (CPU 0.1-32, Memory 128-65536)"
  artifacts:
    - path: "packages/centinela-core/src/centinela/config/models.py"
      provides: "PlatformConfig, SecurityConfig, ResourceLimits with validation"
    - path: "packages/centinela-core/src/centinela/secrets/manager.py"
      provides: "SecretManager with AES-256-GCM encryption, PBKDF2 key derivation"
  key_links:
    - from: "config/models.py"
      to: "models/types.py"
      via: "ResourceQuota import"
    - from: "secrets/manager.py"
      to: "enums.py"
      via: "AuditAction.SECRET_ROTATE"
---

<objective>
Implement config parsing/validation and cryptographic secrets management.

Purpose: Enable declarative security policy management and secure credential handling
Output: Config and secrets modules under centinela-core
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-project-setup-and-core-infrastructure/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create config module with validation</name>
  <files>packages/centinela-core/src/centinela/config/models.py</files>
  <action>
Create config/models.py with:
- ResourceLimits: cpu_cores (0.1-32), memory_mb (128-65536), timeout_seconds (1-3600), disk_mb (optional)
- SecurityConfig: enable_network_isolation (bool), wireguard_enabled (bool), gvisor_enabled (bool), seccomp_profile (str)
- PlatformConfig: namespace (str), resources (ResourceLimits), security (SecurityConfig), audit_enabled (bool)

Use pydantic Field with ge/le constraints. Implement field_validator for env var substitution (${VAR} and ${VAR:-default} patterns).
  </action>
  <verify>PlatformConfig.model_validate({'resources': {'cpu_cores': 2, 'memory_mb': 1024}}) works</verify>
  <done>Config models validate with correct constraints</done>
</task>

<task type="auto">
  <name>Create config module __init__ and loader</name>
  <files>packages/centinela-core/src/centinela/config/__init__.py</files>
  <action>
Create config/__init__.py with:
- from .models import PlatformConfig, ResourceLimits, SecurityConfig
- ConfigLoader class: load from YAML file, load from JSON string, load from dict
- load_config(path: str) -> PlatformConfig convenience function

Use yaml and json modules for parsing (add to dependencies in Plan 1).
  </action>
  <verify>from centinela.config import load_config works</verify>
  <done>Config loads from YAML/JSON files</done>
</task>

<task type="auto">
  <name>Create secrets manager with AES-256-GCM</name>
  <files>packages/centinela-core/src/centinela/secrets/manager.py</files>
  <action>
Create secrets/manager.py with:
- SecretManager class: __init__(master_key: bytes), encrypt(plaintext: bytes) -> tuple[bytes, bytes], decrypt(ciphertext: bytes, nonce: bytes) -> bytes
- derive_key(password: str, salt: bytes, iterations: int = 600000) -> bytes using PBKDF2HMAC
- SecretRef class for tmpfs mount reference
- generate_salt() -> bytes for key derivation
- Auto-rotate method that re-encrypts secrets with new key

Use cryptography.hazmat.primitives.ciphers.aead.AESGCM for encryption.
  </action>
  <verify>SecretManager.encrypt and decrypt round-trip works correctly</verify>
  <done>Secrets encrypt/decrypt with AES-256-GCM</done>
</task>

<task type="auto">
  <name>Create secrets module __init__</name>
  <files>packages/centinela-core/src/centinela/secrets/__init__.py</files>
  <action>
Create secrets/__init__.py with:
- from .manager import SecretManager, derive_key, generate_salt, SecretRef
- SecretStore abstract interface for backend abstraction (SQLite/HashiCorp Vault)
- __all__ exports
  </action>
  <verify>from centinela.secrets import SecretManager works</verify>
  <done>Secrets module exports correctly</done>
</task>

</tasks>

<verification>
- Run `ruff check packages/centinela-core/src/centinela/config/ packages/centinela-core/src/centinela/secrets/`
- Run `mypy packages/centinela-core/src/centinela/config/ packages/centinela-core/src/centinela/secrets/`
- Test config validation: PlatformConfig should reject cpu_cores=0 or cpu_cores=100
</verification>

<success_criteria>
- Config loads from YAML with ${VAR} env var substitution
- Resource limits enforce CPU (0.1-32) and Memory (128-65536) constraints
- Secrets use AES-256-GCM with PBKDF2 key derivation (600k iterations)
- Encryption/decryption round-trips correctly
</success_criteria>

<output>
After completion, create `.planning/phases/05-project-setup-and-core-infrastructure/05-03-SUMMARY.md`
</output>