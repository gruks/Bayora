---
phase: 05-project-setup-and-core-infrastructure
plan: 05
type: execute
wave: 4
depends_on:
  - 05-04
files_modified:
  - packages/centinela-core/src/centinela/anomaly/__init__.py
  - packages/centinela-core/src/centinela/anomaly/detector.py
  - packages/centinela-core/src/centinela/network/__init__.py
  - packages/centinela-core/src/centinela/network/manager.py
autonomous: true

must_haves:
  truths:
    - "Anomaly detector monitors with configurable thresholds"
    - "Network manager handles WireGuard config"
    - "Prometheus metrics export correctly"
  artifacts:
    - path: "packages/centinela-core/src/centinela/anomaly/detector.py"
      provides: "AnomalyDetector with statistical baseline, threshold alerts"
    - path: "packages/centinela-core/src/centinela/network/manager.py"
      provides: "NetworkManager for WireGuard config and network policies"
  key_links:
    - from: "anomaly/detector.py"
      to: "enums.py"
      via: "EventType import"
    - from: "network/manager.py"
      to: "models/types.py"
      via: "NetworkConfig import"
---

<objective>
Implement real-time anomaly detection and network segmentation.

Purpose: Enable security monitoring and WireGuard-based network isolation
Output: Anomaly and network modules under centinela-core
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-project-setup-and-core-infrastructure/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create anomaly detector with statistical monitoring</name>
  <files>packages/centinela-core/src/centinela/anomaly/detector.py</files>
  <action>
Create anomaly/detector.py with:
- AnomalyDetector class: __init__(window_size: int = 100, threshold_sigma: float = 3.0)
- observe(metric_name: str, value: float) -> None: update baseline statistics
- detect(metric_name: str) -> list[dict]: return anomalies above threshold_sigma
- get_baseline(metric_name: str) -> dict: return mean, std, min, max for metric

Use running statistics (Welford's algorithm) for numerical stability. Include thresholds:
- >3σ deviation for timing anomalies
- >10 MB/min for traffic anomalies
- Prometheus metrics for export via prometheus_client
  </action>
  <verify>detect() returns anomalies when values exceed threshold_sigma</verify>
  <done>Anomaly detection works with configurable thresholds</done>
</task>

<task type="auto">
  <name>Create anomaly module __init__</name>
  <files>packages/centinela-core/src/centinela/anomaly/__init__.py</files>
  <action>
Create anomaly/__init__.py with:
- from .detector import AnomalyDetector
- AnomalyAlert model for alert output
- Prometheus metrics: anomaly_count, anomaly_severity
- __all__ exports
  </action>
  <verify>from centinela.anomaly import AnomalyDetector works</verify>
  <done>Anomaly module exports correctly</done>
</task>

<task type="auto">
  <name>Create network manager for WireGuard</name>
  <files>packages/centinela-core/src/centinela/network/manager.py</files>
  <action>
Create network/manager.py with:
- WireGuardConfig model: interface_name, private_key (base64), address (CIDR), dns (list[str]), peers (list[PeerConfig])
- PeerConfig model: public_key, allowed_ips (list[str]), endpoint (str | None), persistent_keepalive (int | None)
- NetworkManager class: 
  - load_config(path: str) -> WireGuardConfig: parse wgconfig-style file using wgconfig library
  - generate_keypair() -> tuple[str, str]: generate private/public key pair
  - apply_policy(policy_type: NetworkPolicyType, tenant_id: str) -> dict: generate Kubernetes NetworkPolicy
  - validate_config(config: WireGuardConfig) -> list[str]: return validation errors

Use wgconfig library for parsing (from Plan 1 dependencies).
  </action>
  <verify>generate_keypair() produces valid base64 key pair</verify>
  <done>Network manager handles WireGuard config</done>
</task>

<task type="auto">
  <name>Create network module __init__</name>
  <files>packages/centinela-core/src/centinela/network/__init__.py</files>
  <action>
Create network/__init__.py with:
- from .manager import NetworkManager, WireGuardConfig, PeerConfig
- NetworkPolicyGenerator for K8s NetworkPolicy creation
- __all__ exports
  </action>
  <verify>from centinela.network import NetworkManager works</verify>
  <done>Network module exports correctly</done>
</task>

</tasks>

<verification>
- Run `ruff check packages/centinela-core/src/centinela/anomaly/ packages/centinela-core/src/centinela/network/`
- Run `mypy packages/centinela-core/src/centinela/anomaly/ packages/centinela-core/src/centinela/network/`
- Test: create anomaly detector, observe 100 values, detect() should return empty; add outlier, detect() should return anomaly
</verification>

<success_criteria>
- Anomaly detector uses statistical baseline (>3σ threshold)
- Network manager parses/generates WireGuard configs
- Key pair generation produces valid base64 keys
- Prometheus metrics export correctly
</success_criteria>

<output>
After completion, create `.planning/phases/05-project-setup-and-core-infrastructure/05-05-SUMMARY.md`
</output>