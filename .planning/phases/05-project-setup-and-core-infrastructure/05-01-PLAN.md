---
phase: 05-project-setup-and-core-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - packages/centinela-core/pyproject.toml
  - tests/conftest.py
autonomous: true

must_haves:
  truths:
    - "All dependencies install without conflicts"
    - "pytest-docker and mockernetes fixtures work"
    - "Code passes ruff and mypy type checking"
  artifacts:
    - path: "pyproject.toml"
      provides: "Core dependencies (kubernetes, docker, fastapi, sqlalchemy, prometheus-client)"
    - path: "pyproject.toml [dev-dependencies]"
      provides: "Testing dependencies (pytest-docker, mockernetes, pytest-asyncio)"
  key_links:
    - from: "pyproject.toml"
      to: "packages/centinela-core"
      via: "uv workspace membership"
---

<objective>
Install core platform dependencies and testing infrastructure for 8 modules.

Purpose: Establish foundation for orchestrator, config, secrets, audit, provenance, anomaly, network, and resources modules
Output: Updated pyproject.toml with all required dependencies, working test fixtures
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-project-setup-and-core-infrastructure/05-RESEARCH.md

# Only reference prior plan SUMMARYs if genuinely needed
</context>

<tasks>

<task type="auto">
  <name>Add core platform dependencies to root pyproject.toml</name>
  <files>pyproject.toml</files>
  <action>
Add the following to [project.dependencies]:
- "kubernetes>=35.0"
- "docker>=7.1"
- "aiodocker>=0.26"
- "fastapi>=0.115"
- "sqlalchemy[asyncio]>=2.0"
- "prometheus-client>=0.25"
- "wgconfig>=1.2"
- "structlog>=24.4"

Add to [project.optional-dependencies] dev:
- "pytest-docker>=3.2"
- "pytest-asyncio>=0.24"
- "mockernetes>=0.2"
- "kmock>=0.7"
- "requests>=2.32"

Keep existing httpx, pydantic, python-dotenv, cryptography that already exist.
  </action>
  <verify>uv sync --all-packages --all-extras succeeds without conflicts</verify>
  <done>All 12 new dependencies install correctly</done>
</task>

<task type="auto">
  <name>Add centinela-core dependencies</name>
  <files>packages/centinela-core/pyproject.toml</files>
  <action>
Check if centinela-core/pyproject.toml exists and add:
- "prometheus-client>=0.25" for metrics support
- "structlog>=24.4" for structured logging

If file doesn't exist, create minimal pyproject.toml with build-system and project metadata matching root.
  </action>
  <verify>uv sync includes centinela-core without errors</verify>
  <done>centinela-core has required dependencies</done>
</task>

<task type="auto">
  <name>Create test conftest.py with fixtures for docker and k8s</name>
  <files>tests/conftest.py</files>
  <action>
Create tests/conftest.py with:
- pytest-docker-compose fixture for docker-compose integration tests
- mockernetes fixture for K8s unit test mocking  
- pytest-asyncio configuration

If tests/conftest.py exists, append fixtures to existing content.
  </action>
  <verify>pytest --collect-only succeeds with new fixtures</verify>
  <done>conftest.py has docker-compose and k8s mock fixtures</done>
</task>

</tasks>

<verification>
- Run `uv sync --all-packages --all-extras` - should complete without errors
- Run `ruff check .` - should pass
- Run `mypy packages/centinela-core/` - should pass (may have ignore_missing_imports for external libs)
</verification>

<success_criteria>
- All 8 new dependencies (kubernetes, docker, aiodocker, fastapi, sqlalchemy, prometheus-client, wgconfig, structlog) install without version conflicts
- 5 new test dependencies (pytest-docker, pytest-asyncio, mockernetes, kmock, requests) install correctly
- ruff and mypy pass on the codebase
</success_criteria>

<output>
After completion, create `.planning/phases/05-project-setup-and-core-infrastructure/05-01-SUMMARY.md`
</output>